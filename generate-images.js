const fs = require('fs');

// Create some dummy images with text to indicate which one is which, or just use base64 gradients
// For now, I will just download placeholders with unsplash source that definitely work in browser
